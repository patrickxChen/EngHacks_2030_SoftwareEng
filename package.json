{
  "name": "uw-engineering-myth-buster-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npx vercel dev --local --yes --local-config vercel.backend.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "firebase-admin": "^12.7.0",
    "openai": "^4.86.1",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@types/node": "^22.13.10",
    "@vercel/node": "^5.1.12",
    "typescript": "^5.8.2",
    "vercel": "^50.28.0",
    "vitest": "^3.0.8"
  }
}
